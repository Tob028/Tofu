name: .NET Core Test and Publish

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build

    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
    - name: Format code
      run: dotnet format Tofu.sln
    - name: Build
      run: dotnet build Tofu.sln --configuration Release
  
  test:
    runs-on: ubuntu-latest
    name: Test
    needs: build

    steps:
    - name: No tests right now
      run: echo "skipping"

  publishNuget:
    runs-on: ubuntu-latest
    name: Publish Tofu to Nuget
    needs: test

    steps:
    - name: Publish to NuGet
      run: |
        dotnet pack Tofu.sln -o out --configuration Release
        dotnet nuget push out/*.nupkg --source https://api.nuget.org/v3/index.json -k ${NUGET_TOKEN}
      env:
        NUGET_TOKEN: ${{ secrets.NUGET_API_KEY }}

  publishGithub:
    runs-on: ubuntu-latest
    name: Publish Tofu to Github
    needs: test

    steps:
    - name: Skipping
      run: echo "skip"
