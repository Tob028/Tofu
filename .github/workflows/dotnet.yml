name: .NET Core Test and Publish

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build

    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
    - name: Format code
      run: dotnet format Tofu.sln
    - name: Build
      run: dotnet build Tofu.sln --configuration Release
  
  test:
    runs-on: ubuntu-latest
    name: Test
    needs: build

    steps:
    - name: No tests right now
      run: echo "skipping"

  publishNuget:
    runs-on: ubuntu-latest
    name: Publish Tofu to Nuget
    needs: test

    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
    - name: Publish to NuGet
      run: dotnet nuget push src/Tofu/bin/Release/net6.0/*.nupkg --source https://api.nuget.org/v3/index.json -k ${NUGET_TOKEN}
      env:
        NUGET_TOKEN: ${{ secrets.NUGET_API_KEY }}

  publishGithub:
    runs-on: ubuntu-latest
    name: Publish Tofu to Github
    needs: test

    steps:
    - name: Skipping
      run: echo "skip"
